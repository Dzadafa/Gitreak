<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.MyWallet" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

    <style name="Theme.MyWallet" parent="Base.Theme.MyWallet" />

    <style name="Theme.MyWallet.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.MyWallet</item>
    </style>
</resources>
